/* ==========================================================
   YEN — site settings. Edit the two values below.
   ========================================================== */
window.YEN_CONFIG = {
  // The email address people can reach you at (shown in the footer, and used
  // if the sign-up form isn't connected to a form service yet).
  email: "hello@yourdomain.com",

  // Optional: paste your Formspree form URL here, e.g. "https://formspree.io/f/abcdwxyz"
  // While this is empty, the "Send" button opens the visitor's email app instead.
  formEndpoint: ""
};
